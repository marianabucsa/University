Task:

	Implement in C ++ a specific data container (ADT) using a certain representation (indicated) and a linked list
	(SLL - single linked list, DLL - double linked list) as a data structure, with the representation of the links on a dynamic table.

	ADT Priority queue - representation in the form of an SLL with pairs (element, priority), ordered by priority (order relation R).